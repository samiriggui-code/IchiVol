[project]
name = "ichivol-engine"
version = "0.0.0"
description = "IchiVol trading engine (indicators, screener, paper, strategy lab)"
requires-python = ">=3.12,<3.13"

[tool.pytest.ini_options]
# pytest.ini remains the primary config; this block documents the pin only.
